node_modules
.pnpm-store

# Local secrets — never commit real keys. .env.example stays tracked.
.env
.env.local
.env.*.local

# Build outputs
dist
.next
*.tsbuildinfo

# Wrangler-generated types (apps/driver-curation, apps/steam-ingest)
worker-configuration.d.ts
.wrangler

# Test/tooling artifacts
coverage
playwright-report
test-results

# Desktop client (apps/desktop) — Rust build output and the fetched PresentMon
# sidecar. The sidecar is pinned + checksummed by scripts/fetch-presentmon.mjs
# rather than committed; Tauri's generated schemas are build artifacts too.
apps/desktop/src-tauri/target
apps/desktop/src-tauri/binaries
apps/desktop/src-tauri/gen
