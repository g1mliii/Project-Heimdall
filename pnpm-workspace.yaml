packages:
  - "apps/*"
  - "packages/*"
allowBuilds:
  esbuild: true
  sharp: true

# Force-patch a transitive advisory so `pnpm audit:deps` stays green.
# next 16.2.6 pulls postcss <8.5.10 (GHSA-qx2v-qp2m-jg93, XSS in stringify);
# 8.5.15 is the patched, 30-day-soaked pin.
overrides:
  postcss: 8.5.15
