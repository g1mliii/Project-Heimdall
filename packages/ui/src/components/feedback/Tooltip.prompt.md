Hover/focus tooltip for metric definitions ("what is a 0.1% low?") and icon buttons.

```jsx
<Tooltip content="The mean of the slowest 0.1% of frames">
  <i data-lucide="circle-help" />
</Tooltip>
```

Keep content short — it renders on a single nowrap line above the trigger.
